# Only the actions in .github/workflows: nothing else here is a dependency Dependabot
# understands. The tags in home/.chezmoiexternal.toml move by hand.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: "ci"
