#!/bin/sh
# op-sa: `op` as the agent's 1Password service account. The token lives in the login keychain
# (item `op-claude-agent`; see the README) and is loaded into this process only. Not typed by an
# agent: it is deliberately outside the Bash sandbox's excluded commands, so it fails there.
export PATH="/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:$PATH"
OP_SERVICE_ACCOUNT_TOKEN="$(security find-generic-password -s op-claude-agent -w)" || exit 1
export OP_SERVICE_ACCOUNT_TOKEN
exec op "$@"
