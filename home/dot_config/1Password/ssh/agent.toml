# Which keys the 1Password SSH agent offers. Without this it offers every key in every vault,
# and GitHub rejects auth after six. https://www.1password.dev/ssh/agent/config

# The commit-signing key (user.signingkey in the git config); git auth itself is HTTPS.
[[ssh-keys]]
item = "GitHubSSH"
vault = "Private"
