# Global gitignore (git's XDG default). Only machine- and editor-level cruft; anything a repo
# might legitimately track belongs in that repo's own .gitignore.

# macOS
.DS_Store
._*

# nvim's write backup
*~

# Claude Code machine-local state
**/.claude/settings.local.json
**/.claude/.cc-writes/
**/.claude/worktrees/
