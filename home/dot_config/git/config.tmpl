# ~/.config/git/config, one template. Apple's git already sets credential.helper=osxkeychain and
# init.defaultBranch=main in its bundled gitconfig; the github.com helper below severs the
# keychain for GitHub (an empty helper resets the list) so gh alone serves it.
{{ if eq .chezmoi.os "darwin" -}}
# macOS: commits and tags signed with the 1Password SSH key, behind Touch ID. A container signs
# nothing.
[commit]
	gpgSign = true
[tag]
	gpgSign = true
[gpg "ssh"]
	program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign

{{ end -}}
[alias]
  hist = log --oneline --graph --decorate
  sw = switch
  st = status -sb
  undo = reset HEAD~1 --mixed

[user]
	name = alxjrvs
	email = alxjrvs@gmail.com
	# The public half of the 1Password "GitHubSSH" item.
	signingkey = key::ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKjF9JmW396EbH0n1zWeU7eT+2VvMFZAn+A1WKAi4tqT
# github.com through gh's token (what `gh auth setup-git` writes).
[credential "https://github.com"]
	helper =
	helper = !gh auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !gh auth git-credential
[core]
	editor = nvim
	pager = delta
	untrackedCache = true

[init]
	defaultBranch = main
[commit]
	verbose = true
[push]
	autoSetupRemote = true
	followTags = true
	useForceIfIncludes = true
[pull]
	rebase = true
[rerere]
	enabled = true
	autoupdate = true
[interactive]
	diffFilter = delta --color-only
[delta]
	navigate = true
	side-by-side = true
	dark = true
	hyperlinks = true
[fetch]
	prune = true
	all = true
	writeCommitGraph = true
[gc]
	# Agent worktrees churn daily; forget a hand-deleted one after a week, not three months.
	worktreePruneExpire = 1.week.ago

[merge]
	conflictstyle = zdiff3
[branch]
	sort = -committerdate
[column]
	ui = auto
[diff]
	algorithm = histogram
	colorMoved = dimmed-zebra
	mnemonicPrefix = true
[rebase]
	autoSquash = true
	autoStash = true
	updateRefs = true
	missingCommitsCheck = error
[tag]
	sort = version:refname
[gpg]
	format = ssh
[help]
	autocorrect = prompt
