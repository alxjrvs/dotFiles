# Only what atuin's history shows typed.

# Git
alias gs="git status"
alias gp="git push"
alias gpr='git pull --rebase'
alias gco='git checkout'
alias gd="git diff"

# Navigation
alias ..="cd .."

# `cat` stays POSIX cat.
alias ls="eza --icons --group-directories-first"

# Editor
alias vim="nvim"
