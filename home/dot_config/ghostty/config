# Ghostty.app config — the sole terminal. Read from ~/.config/ghostty/config
# (a fixed path, no override).

# Theme
theme = Nord

# Font
font-family = FiraCode Nerd Font
font-thicken = true
font-size = 14

# Ghostty 1.2.0 disabled `dlig` only; liga/calt remain on by default.
font-feature = +dlig
adjust-cell-height = -1

# Cursor
mouse-hide-while-typing = true

# Window
window-padding-x = 0
window-padding-y = 0
macos-titlebar-style = native
window-save-state = always
quit-after-last-window-closed = true
macos-option-as-alt = left

# Clipboard
copy-on-select = true

# Quick terminal — drop-down visor toggled by a global hotkey. Pairs with
# Raycast for zero-latency context switching; no app launch needed for a
# 10-second command. `global:` makes the keybind work even when Ghostty
# isn't focused.
quick-terminal-screen = mouse
quick-terminal-animation-duration = 0.15

# Keybindings
keybind = ctrl+shift+w=close_surface
keybind = global:ctrl+grave_accent=toggle_quick_terminal
