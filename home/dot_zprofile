# Login shells. /etc/zprofile's path_helper rebuilds PATH after ~/.zshenv, so the two tool
# directories go back in front; brew shellenv also exports FPATH.
[[ -x /opt/homebrew/bin/brew ]] && eval "$(/opt/homebrew/bin/brew shellenv)"
path=("$HOME/.local/bin" "$HOME/.local/share/mise/shims" $path)
