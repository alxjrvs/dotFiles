# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
*.swp
*.swo
*~
.idea/
.vscode/

# Stray editor/tool backups — never committed
*.bak
*.bak-*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude — this repo's own project-scoped `.claude/` is machine-local; the
# user/global config lives in `dot-claude/`, symlinked into ~/.claude/ by
# `boom source`. (Repo-scoped only — global excludes live in `git/ignore`, NOT
# a blanket `.claude/` that would hide it in every repo on the machine.)
#
# `.claude/*` rather than `.claude/` on purpose: git never descends into an
# excluded *directory*, so a `!` negation inside one is silently inert. Ignoring
# the contents instead is what lets the one exception below actually apply.
.claude/*
# The one tracked exception: telemetry attribution is a team-shared property of
# the repo, not a machine-local preference, so `project.name` is checked in and
# every clone emits the same tag with no per-member setup. `settings.local.json`
# stays ignored (line below) and `worktrees/` stays ignored by `.claude/*`.
!.claude/settings.json

# Transient/generated
Brewfile.lock.json

# Machine-local override — NOT a pattern this setup uses (every divergence lives
# in the committed settings.json contract). Claude Code auto-writes one on
# "always allow"/project-trust flows, so this line stops it being COMMITTED; it
# never stopped it EXISTING, and one carrying `Bash(gh api *)` sat unreviewed
# because git was told to ignore it. boom's `absent` resource closes that
# half — removed on every `boom source`, asserted absent by `boom verify`.
dot-claude/settings.local.json
**/.claude/settings.local.json
