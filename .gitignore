# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
*.swp
*.swo
*~

# Claude Code's machine-local state: settings.local.json, worktrees/, .cc-writes/. Nothing
# under here is a property of the repo, so the whole directory goes.
.claude/
