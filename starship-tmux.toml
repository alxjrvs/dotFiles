# Minimal starship config for use inside tmux
# Git info shown in pane borders; prompt is just the default character.

format = "$character"
add_newline = false

[character]
success_symbol = "[❯](white)"
error_symbol = "[❯](red)"

[cmd_duration]
disabled = true
