# agent-vault.txt — the items in the `claude-agent` 1Password vault that THIS REPO
# RESOLVES.
#
# Documentation, not a gate: every consumer below reports a missing item on first use.
#
# Adding a line means naming what resolves it, in the same commit. Titles must be
# kebab-case: every op:// ref is re-parsed by `sh -c`, so a space word-splits and
# the resolve fails silently — the bug that took down two MCP servers on
# 2026-07-25.
#
# Why membership is the lever at all, and why the vault is kept small: service
# account vault access is IMMUTABLE after creation, so scope can never be
# tightened in place — only the vault's contents can change. Reasoning, and the
# 2026-08-19 reduction that shrank it: dot-claude/DECISIONS.md.

claude-git-pat            # git credential.helper (agent pushes) — its only consumer
gninety                   # NINETY_API_TOKEN_COMMAND in dot-claude/settings.json
npm-publish-token         # npm/publish.env -> `op run --env-file=npm/publish.env -- npm publish`
