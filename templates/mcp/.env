# References only — safe to commit. `op run --env-file=.env` (the command in
# .mcp.json) resolves these into the MCP server process at launch; no secret
# value ever rests on disk. NEVER put a resolved value here — only op:// refs.
EXAMPLE_API_KEY=op://claude-agent/Example API Key/credential
