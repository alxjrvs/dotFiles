# Machine-local secrets — NOT in git
# Copy to .secrets (done automatically by sync.sh)
# Fill in values or leave blank to skip


# npm registry auth token (get from npmjs.com/settings/~/tokens)
export NPM_TOKEN=""
