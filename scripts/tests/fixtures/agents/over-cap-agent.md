---
name: over-cap-agent
description: This subagent description exists only to be too long, because a cap that is never exercised against the shape it claims to cover is not a cap at all but a hope, and the whole reason this fixture exists is that dot-claude/agents was linked into the home directory and billed to every single session for a long while without any gate whatsoever asserting one single thing about how many words its frontmatter description was allowed to spend on saying what it does, which is precisely the drift that the renamed description-cap script now closes for both shapes at once.
---

# over-cap-agent

Body content is never billed, so its length is irrelevant to this fixture.
